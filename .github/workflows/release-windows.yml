name: Release Windows Host

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install

      - name: Build protocol
        run: npm run build:protocol

      - name: Generate app icon (valid .ico for Windows)
        shell: pwsh
        working-directory: apps/runner
        run: |
          Add-Type -AssemblyName System.Drawing
          $path = "app-icon.png"
          $bmp = New-Object System.Drawing.Bitmap 512, 512
          $g = [System.Drawing.Graphics]::FromImage($bmp)
          $g.Clear([System.Drawing.Color]::FromArgb(16, 185, 129))
          $font = New-Object System.Drawing.Font("Arial", 48, [System.Drawing.FontStyle]::Bold)
          $brush = New-Object System.Drawing.SolidBrush([System.Drawing.Color]::White)
          $g.DrawString("AI", $font, $brush, 200, 220)
          $g.Dispose()
          $bmp.Save($path, [System.Drawing.Imaging.ImageFormat]::Png)
          $bmp.Dispose()
          npm exec tauri icon $path

      - name: Build Tauri installer (.exe NSIS)
        working-directory: apps/runner
        env:
          VITE_SUPABASE_URL: https://jcknolulyrsvcwvttaed.supabase.co
          VITE_APP_URL: https://ownmyownai-app-runner.vercel.app
        run: npm run tauri build

      - name: Find installer
        id: installer
        shell: bash
        run: |
          FILE=$(find apps/runner/src-tauri/target/release/bundle/nsis -name "*-setup.exe" 2>/dev/null | head -1)
          if [ -z "$FILE" ]; then
            FILE=$(find apps/runner/src-tauri/target/release/bundle/nsis -name "*.exe" | head -1)
          fi
          if [ -z "$FILE" ]; then
            echo "No NSIS installer found"
            exit 1
          fi
          echo "path=$FILE" >> "$GITHUB_OUTPUT"
          echo "Found: $FILE"

      - name: Set release tag
        id: tag
        shell: bash
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "name=v0.1.${{ github.run_number }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          name: OwnMyOwnAI Host ${{ steps.tag.outputs.name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: ${{ steps.installer.outputs.path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

name = "ownmyownai-relay"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[durable_objects]
bindings = [
  { name = "HOST_ROOM", class_name = "HostRoom" }
]

# Plan Workers gratuit : Durable Objects SQLite obligatoires (new_sqlite_classes)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["HostRoom"]

[vars]
# Set RELAY_JWT_SECRET via wrangler secret put RELAY_JWT_SECRET
